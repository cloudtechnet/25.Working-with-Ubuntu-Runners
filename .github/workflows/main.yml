name: Checkout Example

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    
    steps:
      - name: Check Installed Tools
        run: |
          git --version
          node --version
          python --version
          docker --version
